const {groups} = require('./colorCard')
module.exports = {
  "plugins": [
    'tailwindcss',
    'autoprefixer',
    // require('postcss-nested'),
    // require('postcss-nesting'),
    // require('./themePlugin')({
    //   groups
    // })
  ]
}